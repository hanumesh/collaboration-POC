import { FilterByPriorityPipe } from './filter-by-priority.pipe';

describe('FilterByPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
